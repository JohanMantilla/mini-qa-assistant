# Dependencias principales para testing, se deben correr localmente
# pytest==8.3.3
# pytest-asyncio==0.24.0
# httpx==0.27.2
# pytest-asyncio
# reportlab

# Nota: es necesario al probar localmente también tener las dependencias de requirements.txt
# Ejemplo para instar: py -m pip install pytest-asyncio httpx reportlab
