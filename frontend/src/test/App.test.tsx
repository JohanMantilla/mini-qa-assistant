/*
import { test, expect } from 'vitest'
import { render } from '@testing-library/react'
import App from './App'

test('App renders without crashing', () => {
  const { container } = render(<App />)
  expect(container).toBeTruthy()
})

test('App shows main title', () => {
  const { container } = render(<App />)
  const title = container.querySelector('h1')
  expect(title?.textContent).toContain('Mini Asistente Q&A')
})

test('App shows uploader section', () => {
  const { container } = render(<App />)
  const uploader = container.querySelector('.file-uploader')
  expect(uploader).toBeTruthy()
})

  test('should show file uploader section', () => {
    const { container } = render(<App />)
    
    const uploaderSection = container.querySelector('.file-uploader')
    expect(uploaderSection).toBeInTheDocument()
  })

  test('should show indexed documents section', () => {
    const { container } = render(<App />)
    
    const indexedSection = container.querySelector('.indexed-documents')
    expect(indexedSection).toBeInTheDocument()
  })
*/